



Thread(쓰레드)
- 작업 흐름

앱실행 ----> Launcher Activity ----> -----> ----> 작업흐름

안드로이드의 쓰레드
-> MainThread

쓰레드가 여려 개 있는 경우 -> 여러 가지 일을 한 번에 할 수 있다

안드로이드 MainThread의 특징
- UI Thread
    -> 사용자의 input을 받는 Thread
- 절대 정지시킬 수 없다!!(하면 안된다!)
    -> 왜나하면, 정지시키거나 종료 시키면 더 이상 사용자의 input을 받을 수 없기 때문이다
